# Music Assets

This directory (`frontend/public/music`) is where you should place your BGM files (.mp3).

Please rename your files to match the paths defined in `frontend/app/timer/page.tsx` (e.g., `bgm1.mp3`, `bgm2.mp3`) or update the `MUSIC_TRACKS` constant in that file.
